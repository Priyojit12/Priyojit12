import time
from plyer import notification
def notify():
    if __name__ == "__main__":
        notification.notify(
            title = "waterreminder",
            message = "DRINK WATER FAST",
            timeout = 1
    )
        time.sleep(36)
        return notify()
notify()
